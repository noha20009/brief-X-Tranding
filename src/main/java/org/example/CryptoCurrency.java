package org.example;

public class CryptoCurrency extends Asset {
    public CryptoCurrency(int idAsset, String nom, double prixUnitaire, String type) {
        super(idAsset, nom, prixUnitaire, type);
    }
}
